specVersion: "v1"
serviceId: "deneme"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "deneme-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
